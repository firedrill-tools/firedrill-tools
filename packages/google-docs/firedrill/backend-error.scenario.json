{
  "schemaVersion": 1,
  "id": "backend-error",
  "title": "A provider outage: two read paths and the batch update fail before anything is written, so the document is byte-identical afterwards.",
  "faults": [
    {
      "packageId": "google-docs",
      "faultId": "backend-error"
    }
  ]
}
