{
  "schemaVersion": 1,
  "id": "lost-update",
  "title": "The lost-update trap: the batch update commits but answers 503, so a blind retry writes the edit twice.",
  "faults": [
    {
      "packageId": "google-docs",
      "faultId": "lost-update-reply"
    }
  ]
}
