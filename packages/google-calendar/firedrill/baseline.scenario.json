{
  "schemaVersion": 1,
  "id": "baseline",
  "title": "Baseline: two fictional users, a shared team calendar, a read-only holiday calendar and a meeting room exactly as authored in world.json (no overrides, no faults)"
}
