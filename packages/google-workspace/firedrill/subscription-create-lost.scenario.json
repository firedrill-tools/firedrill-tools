{
  "schemaVersion": 1,
  "id": "subscription-create-lost",
  "title": "A subscription is committed but the caller sees 500 INTERNAL, so the naive retry answers 409",
  "faults": [
    {
      "packageId": "google-workspace",
      "faultId": "subscription-create-lost"
    }
  ]
}
